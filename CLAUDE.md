# إرشادات العمل على مشروع إنابة (Enaba)

## المرجع الوحيد

**[`SPEC.md`](./SPEC.md) هو المستند المرجعي الوحيد والنهائي لهذا المشروع.**

- اقرأه بالكامل قبل كتابة أي سطر كود أو اتخاذ أي قرار تصميمي أو معماري.
- **كل مواصفة أو برومبت أو مستند سابق ملغى بالكامل** ولا يُعتد به — بما في ذلك برومبت الإصدار الأول (v1).
- عند أي تعارض بين تعليمات واردة في محادثة وبين `SPEC.md`، **`SPEC.md` هو الحاكم**.
- أي تغيير في النطاق أو المعمارية لا يُنفَّذ في الكود قبل تحديث `SPEC.md` أولًا في نفس التغيير (PR).

## قرارات نهائية غير قابلة للتفاوض

هذه القرارات مُثبتة في [§3 من SPEC.md](./SPEC.md#3-القرارات-المعمارية-الملزِمة). لا تُقترح بدائل لها:

| القرار | الحالة |
|---|---|
| **Next.js 15 (App Router)** | إلزامي — **Vite ممنوع** |
| **Supabase Postgres** كمخزن البيانات | إلزامي — **LocalStorage كمخزن بيانات ممنوع** |
| **RLS مفعّل على كل جدول** | إلزامي بلا استثناء |
| **توثيق الكارنيه يدويًا قبل الظهور في الدليل** | إلزامي — لا يوجد وضع «اختياري في MVP» |
| **إشعارات Web Push** | شرط إطلاق، وليست ميزة إضافية |
| TypeScript `strict` · `any` ممنوع | إلزامي |

`localStorage` يُستخدم **فقط** لتفضيلات الواجهة (المحافظة الافتراضية، إغلاق شريط التنويه،
إغلاق البانر السفلي) — ولا يُستخدم لتخزين محامين أو طلبات أو أي بيانات مشتركة.

## قواعد أمنية حرجة

1. **`SUPABASE_SERVICE_ROLE_KEY` لا يُستورد إطلاقًا** في أي ملف يحتوي `"use client"`.
   أي متغير بلا بادئة `NEXT_PUBLIC_` هو متغير خادم فقط.
2. **أرقام الهواتف لا تُرسَل ضمن HTML الصفحة أبدًا.** تُقرأ الواجهة من العرض `public_lawyers`
   الذي لا يحتوي أعمدة الاتصال، ويُجلب الرقم فرديًا عبر `app/api/contact/route.ts` مع تحديد معدل.
3. **صور كارنيهات النقابة في bucket خاص.** تُعرض للمشرف فقط عبر رابط موقّع صالح 5 دقائق.
4. **لا يُخزَّن عنوان IP خام.** تُخزَّن بصمة `SHA256(ip + user_agent + DAILY_HASH_SALT)`.
5. **`/admin` محمي بطبقتين مستقلتين:** `middleware.ts` + سياسات RLS. إخفاء الرابط ليس أمانًا.
6. سياسة `with check` على `lawyer_profiles` يجب أن تمنع المستخدم من تعديل `role` أو
   `verification_status` الخاصين به (رفع صلاحيات / توثيق ذاتي).

## قواعد الواجهة

- الاتجاه `dir="rtl"` و `lang="ar"` على مستوى `<html>`.
- **كل مكوّن قائمة يجب أن يغطي ثلاث حالات:** تحميل (Skeleton) · فارغة (رسالة + إجراء) · خطأ (رسالة + إعادة محاولة).
  مكوّن قائمة بدونها لا يُقبل.
- كل عنصر تفاعلي **44×44 بكسل كحد أدنى** — الاستخدام بإصبع واقفًا داخل المحكمة.
- الافتراضي هو Server Component. `"use client"` في أضيق نطاق ممكن.
- الرموز اللونية من `@theme` في [§4](./SPEC.md#4-نظام-التصميم) — **ممنوع** كتابة ألوان مباشرة في المكوّنات.
- روابط واتساب تُبنى حصريًا عبر `lib/phone.ts` (تطبيع E.164 ثم `wa.me/20XXXXXXXXX`).

## قبل أي دمج (Merge Blockers)

- [ ] `tsc --noEmit` نظيف
- [ ] `next lint` نظيف
- [ ] RLS مفعّل ومختبر على أي جدول جديد
- [ ] لا مفاتيح سرية في كود العميل
- [ ] الحالات الثلاث موجودة في أي قائمة جديدة
- [ ] Lighthouse للجوال ≥ 90 (أداء · إمكانية وصول · SEO)
- [ ] اختبارات القبول ذات الصلة في [§16](./SPEC.md#16-معايير-الجودة-والقبول) تمر

## نبرة المحتوى العربي

- **ممنوع** أي صياغة تسويقية مقارنة: «الأفضل»، «الأشهر»، «الأسرع»، أو أي ضمان لنتيجة —
  قيود الإعلان في قانون المحاماة 17/1983 (راجع [§13.2](./SPEC.md#132-قانون-المحاماة-171983--قيود-الإعلان)).
- الموضع اللغوي الثابت: **«دليل مهني لتبادل الإنابات بين الزملاء»** — وليس «إعلانات محامين».
- شريط التنويه القانوني نصه مثبت في [§8/F1](./SPEC.md#f1--الهيدر-والتنويه-القانوني) ولا يُعاد صياغته.
